import {
  Swiper,
  EffectFade,
  Autoplay,
  Thumbs,
  Pagination,
  Navigation,
} from 'swiper/js/swiper.esm.js';

Swiper.use([EffectFade, Autoplay, Pagination, Navigation, Thumbs]);

export default Swiper;
